# Figma translate plugin with DeepL proxy server

## Usage

Configure DeepL API key in `.env` as provided in `sample.env`

Run proxy server:
```bash
go run .
```

Run plugin from manifest in Figma and configure the server address.